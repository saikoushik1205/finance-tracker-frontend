Fintrack Backend - Docs Index

This folder contains operational and deployment notes for the backend.

- `ecosystem.config.js` and `vercel.json` are in `ops/`.
- `scripts/` contains maintenance helpers (seed-test-user.js, generateSecret.js).

Keep `server.js`, `package.json`, and `src/` at the project root for runtime and development.
